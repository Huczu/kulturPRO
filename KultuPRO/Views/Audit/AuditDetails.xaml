<Window x:Class="KulturPRO.Views.Audit.AuditDetails"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:KulturPRO.ViewModels"
        xmlns:converters="clr-namespace:KulturPRO.Utillities.AuditLogConverters"
        Title="AuditDetails" Height="300" Width="300"
        ResizeMode="NoResize">

    <Window.Resources>
        <converters:ClassTypeConverter x:Key="classTypeConverter" />
        <converters:EventTypeConverter x:Key="eventTypeConverter" />
    </Window.Resources>

    <Grid>
        <TextBlock x:Name="ChangedBlock" Text="Zmiana w:" Margin="0,0,198,245" />
        <TextBlock x:Name="TypeFullName" Text="{Binding AuditLog.TypeFullName, Converter={StaticResource classTypeConverter}}" Margin="99,0,82,245" />
        
        <TextBlock x:Name="DateBlock" Text="Data zmiany:" Margin="0,30,198,212" />
        <TextBlock x:Name="EventDateUTC" Text="{Binding AuditLog.EventDateUTC}" Margin="99,30,82,212" />
    </Grid>
</Window>
